## docker-vdmpp
- target: vdmpp v\_9.0.7

## usage
e.g.
to check all vdmpp files in /tmp...
```
command: /bin/bash -c "vppde -t $$(find /tmp -name \"*.vdmpp\")"
```

run vppgde
```
cd example
docker-compose up -d
```
